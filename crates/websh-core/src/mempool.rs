//! Public mempool facade.
//!
//! This module owns the canonical mempool categories, path conventions,
//! frontmatter parsing, compose validation, and manifest-entry rules shared by
//! the CLI and browser.

pub use crate::engine::mempool::*;
