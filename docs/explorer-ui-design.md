# File Explorer UI Design

## 개요

터미널 인터페이스의 모바일 접근성 문제를 해결하기 위한 파일 탐색기 UI.
터미널의 미학(모노스페이스, 다크 테마, 터미널 컬러)을 유지하면서 터치 친화적인 경험 제공.

## 디자인 원칙

- **모바일 우선**: 터치 타겟, 스와이프 제스처, 바텀 시트 등 모바일 네이티브 패턴 활용
- **터미널 미학 유지**: 퍼미션 표기(`drwx`, `-rw-`), 모노스페이스 폰트, 기존 컬러 팔레트
- **단순함**: 리스트 뷰 기본, 필요시 그리드 토글
- **점진적 공개**: 미리보기는 바텀 시트로, 필요할 때만 표시

## 레이아웃

### 기본 상태 (리스트 뷰)

```
┌─────────────────────────────────────────┐
│  ← ~/projects                      [>_] │  ← Fixed Header
├─────────────────────────────────────────┤
│                                         │
│  drwx  projects/                      > │
│  drwx  writings/                      > │
│  -rw-  about.md                    1.2K │
│  -rw-  resume.pdf                  240K │
│  -rw-  private_key.enc         🔒  0.8K │  ← 암호화 파일
│                                         │
│                         [ ☰ List | ⊞ ] │  ← 뷰 토글
└─────────────────────────────────────────┘
```

### 그리드 뷰

```
┌─────────────────────────────────────────┐
│  ← ~/projects                      [>_] │
├─────────────────────────────────────────┤
│                                         │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  │
│  │  drwx   │  │  drwx   │  │  -rw-   │  │
│  │projects/│  │writings/│  │about.md │  │
│  │         │  │         │  │  1.2K   │  │
│  └─────────┘  └─────────┘  └─────────┘  │
│                                         │
│  ┌─────────┐  ┌─────────┐               │
│  │  -rw-   │  │🔒 -rw-  │               │
│  │resume.  │  │private_ │               │
│  │  240K   │  │  0.8K   │               │
│  └─────────┘  └─────────┘               │
│                                         │
│                         [ ☰ | ⊞ Grid ] │
└─────────────────────────────────────────┘
```

### 파일 선택 시 (바텀 시트)

```
┌─────────────────────────────────────────┐
│  ← ~/projects                      [>_] │
├─────────────────────────────────────────┤
│                                         │
│  drwx  projects/                      > │
│  drwx  writings/                      > │
│  -rw-  about.md                    1.2K │  ← 선택됨 (하이라이트)
│  -rw-  resume.pdf                  240K │
│  -rw-  private_key.enc         🔒  0.8K │
│                                         │
└─────────────────────────────────────────┘
┌ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┐  ← 드래그 핸들
│  about.md                          [↑] │
├─────────────────────────────────────────┤
│  # About Me                             │
│  Software engineer interested in...     │
└─────────────────────────────────────────┘
```

### 바텀 시트 전체 확장

```
┌─────────────────────────────────────────┐
│  [↓]  about.md                     [>_] │  ← 닫기/최소화 버튼
├─────────────────────────────────────────┤
│                                         │
│  # About Me                             │
│                                         │
│  Software engineer interested in        │
│  cryptography and decentralized         │
│  systems.                               │
│                                         │
│  ## Experience                          │
│                                         │
│  - Company A (2022-present)             │
│  - Company B (2020-2022)                │
│                                         │
│  ## Projects                            │
│  ...                                    │
│                                         │
└─────────────────────────────────────────┘
```

### 암호화된 파일 선택 시

```
┌─────────────────────────────────────────┐
│  ← ~/projects                      [>_] │
├─────────────────────────────────────────┤
│                                         │
│  ...                                    │
│  -rw-  private_key.enc         🔒  0.8K │  ← 선택됨
│                                         │
└─────────────────────────────────────────┘
┌ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┐
│  private_key.enc               [Decrypt] │  ← 복호화 버튼
├─────────────────────────────────────────┤
│                                         │
│  🔒 This file is encrypted              │
│                                         │
│  Size: 0.8K                             │
│  Modified: 2024-01-15                   │
│                                         │
└─────────────────────────────────────────┘
```

## 컴포넌트 상세

### Header

| 요소 | 설명 |
|------|------|
| `←` | 뒤로가기 (상위 디렉토리), 루트에서는 숨김 |
| `~/projects` | 현재 경로, 탭하면 경로 네비게이션 |
| `[>_]` | 터미널 모드 전환 버튼 |

### 파일 리스트 아이템

| 요소 | 설명 |
|------|------|
| `drwx` / `-rw-` | 퍼미션 표기 (디렉토리/파일 구분) |
| 파일명 | 이름 + 확장자 |
| `🔒` | 암호화 파일 표시 (선택적) |
| `1.2K` | 파일 크기 (파일만) |
| `>` | 디렉토리 진입 표시 (폴더만) |

### 바텀 시트

| 상태 | 높이 | 동작 |
|------|------|------|
| 닫힘 | 0% | 파일 미선택 상태 |
| 미리보기 | 30-40% | 파일 탭 시 기본 상태 |
| 전체화면 | 100% | 드래그 업 또는 [↑] 버튼 |

### 뷰 토글

| 모드 | 아이콘 | 설명 |
|------|--------|------|
| List | `☰` | 기본값, 세로 목록 |
| Grid | `⊞` | 카드 그리드 |

## 인터랙션

### 탭 동작

| 대상 | 동작 |
|------|------|
| 폴더 | 해당 폴더로 이동 |
| 파일 | 바텀 시트 올라옴 (미리보기) |
| 암호화 파일 | 바텀 시트 + 복호화 버튼 표시 |

### 제스처

| 제스처 | 동작 |
|--------|------|
| 스와이프 오른쪽 (엣지) | 뒤로가기 (상위 디렉토리) |
| 바텀 시트 드래그 업 | 전체화면 확장 |
| 바텀 시트 드래그 다운 | 닫기 |

### 키보드 (PC)

| 키 | 동작 |
|----|------|
| `↑` / `↓` | 파일 선택 이동 |
| `Enter` | 폴더 진입 / 파일 열기 |
| `Backspace` | 상위 디렉토리 |
| `Cmd/Ctrl + \`` | 터미널 모드 전환 |

## 색상 (기존 터미널 팔레트)

| 용도 | 변수 | 색상 |
|------|------|------|
| 배경 | `--bg-primary` | `#0d1117` |
| 폴더명 | `--terminal-cyan` | `#58a6ff` |
| 파일명 | `--text-primary` | `#c9d1d9` |
| 퍼미션/크기 | `--text-dim` | `#6e7681` |
| 선택 하이라이트 | `--bg-secondary` | `#161b22` |
| 암호화 아이콘 | `--terminal-yellow` | `#d29922` |

## 반응형

| 브레이크포인트 | 동작 |
|----------------|------|
| Mobile (< 768px) | 리스트 뷰 기본, 바텀 시트 미리보기 |
| Tablet (768-1024px) | 그리드 옵션, 바텀 시트 또는 사이드 패널 |
| Desktop (> 1024px) | 듀얼 패널 옵션 (리스트 + 미리보기) |

## 터미널 연동

- `[>_]` 버튼으로 언제든 터미널 모드 전환
- 탐색기에서 이동한 경로는 터미널 PWD와 동기화
- 터미널에서 `cd`, `ls` 등 실행 시 탐색기 상태도 업데이트

---

## 구현 아키텍처

### 뷰 모드 분리

Terminal과 Explorer를 독립적인 뷰로 분리하고, Reader는 오버레이로 처리.

```rust
/// 메인 뷰 모드 (Terminal 또는 Explorer)
pub enum ViewMode {
    Terminal,
    Explorer,
}

/// 컨텐츠 오버레이 (Reader 등)
pub enum ContentMode {
    None,
    Reader { content: String, title: String },
    // 향후 Editor 등 추가 가능
}
```

### AppContext 확장

```rust
pub struct AppContext {
    // 기존 (공유 상태)
    pub terminal: TerminalState,
    pub wallet: RwSignal<WalletState>,
    pub fs: RwSignal<VirtualFs>,

    // 뷰 관리 (추가)
    pub view_mode: RwSignal<ViewMode>,
    pub content_mode: RwSignal<ContentMode>,

    // Explorer 전용 (추가)
    pub explorer: ExplorerState,
}

pub struct ExplorerState {
    pub selected_file: RwSignal<Option<String>>,
    pub view_type: RwSignal<ExplorerViewType>,  // List / Grid
    pub sheet_state: RwSignal<SheetState>,      // Closed / Preview / Expanded
}
```

### 공유 상태

Terminal과 Explorer가 동일한 상태를 참조하여 자동 동기화:

| 상태 | 공유 | 용도 |
|------|------|------|
| `fs` | ✅ | 파일시스템 (읽기/쓰기) |
| `terminal.current_path` | ✅ | 현재 디렉토리 (cd 동기화) |
| `wallet` | ✅ | 권한 계산 |
| `terminal.history` | ❌ | Terminal 전용 |
| `explorer.selected_file` | ❌ | Explorer 전용 |

### 컴포넌트 구조

```
src/
├── app.rs                  # AppContext 정의 + provide
└── components/
    ├── mod.rs              # 모듈 re-export
    ├── shell.rs            # 루트 (뷰 전환 로직)
    ├── chrome/             # 뷰와 무관하게 항상 표시되는 UI
    │   ├── mod.rs
    │   ├── status.rs       # 상단 상태바
    │   └── mode_switch.rs  # [>_] / [📁] 전환 버튼
    ├── terminal/           # 터미널 뷰
    │   ├── mod.rs
    │   ├── input.rs
    │   └── output.rs
    ├── explorer/           # 탐색기 뷰 (신규)
    │   ├── mod.rs
    │   ├── list.rs         # 리스트 뷰
    │   ├── grid.rs         # 그리드 뷰
    │   └── sheet.rs        # 바텀 시트 (미리보기)
    └── reader/             # 컨텐츠 오버레이
        └── mod.rs
```

### Shell 컴포넌트 구조

```rust
#[component]
pub fn Shell() -> impl IntoView {
    let ctx = use_context::<AppContext>().expect("AppContext required");

    view! {
        <div class=css::screen>
            <Status />

            <div class=css::main>
                // 메인 뷰 (Terminal 또는 Explorer)
                {move || match ctx.view_mode.get() {
                    ViewMode::Terminal => view! { <Terminal /> }.into_any(),
                    ViewMode::Explorer => view! { <Explorer /> }.into_any(),
                }}

                // 컨텐츠 오버레이 (Reader)
                {move || match ctx.content_mode.get() {
                    ContentMode::None => view! {}.into_any(),
                    ContentMode::Reader { content, title } => {
                        view! {
                            <Reader
                                content_path=content
                                title=title
                                on_close=move |_| ctx.content_mode.set(ContentMode::None)
                            />
                        }.into_any()
                    }
                }}
            </div>
        </div>
    }
}
```

### 경로 동기화

`current_path`는 공유 signal이므로 어느 쪽에서 변경해도 자동 동기화:

```rust
// Terminal에서 cd 실행
ctx.terminal.current_path.set(new_path.clone());
// → Explorer도 같은 signal을 참조하므로 자동 반영

// Explorer에서 폴더 클릭
ctx.terminal.current_path.set(new_path.clone());
// → Terminal의 PWD도 자동 변경
```

### URL 라우팅

```rust
pub enum Route {
    Home,                           // / → Terminal (기본)
    Explorer,                       // /explore → Explorer
    Read { path: String },          // /read/blog/hello.md → Reader 오버레이
}

// URL 변경 시 뷰 모드 설정
match Route::current() {
    Route::Home => ctx.view_mode.set(ViewMode::Terminal),
    Route::Explorer => ctx.view_mode.set(ViewMode::Explorer),
    Route::Read { path } => {
        ctx.content_mode.set(ContentMode::Reader {
            content: path.clone(),
            title: extract_title(&path),
        });
    }
}
```

### 뷰 전환 흐름

```
┌─────────────────────────────────────────────────────────┐
│                        Shell                            │
│  ┌───────────────────────────────────────────────────┐  │
│  │ view_mode = Terminal                              │  │
│  │ ┌─────────────────────────────────────────────┐   │  │
│  │ │              <Terminal />                   │   │  │
│  │ │  - history, input, output                   │   │  │
│  │ │  - current_path 공유                        │   │  │
│  │ └─────────────────────────────────────────────┘   │  │
│  └───────────────────────────────────────────────────┘  │
│                         │                               │
│                    [>_] 클릭                            │
│                         ▼                               │
│  ┌───────────────────────────────────────────────────┐  │
│  │ view_mode = Explorer                              │  │
│  │ ┌─────────────────────────────────────────────┐   │  │
│  │ │              <Explorer />                   │   │  │
│  │ │  - file list, bottom sheet                  │   │  │
│  │ │  - current_path 공유 (동기화됨)             │   │  │
│  │ └─────────────────────────────────────────────┘   │  │
│  └───────────────────────────────────────────────────┘  │
│                         │                               │
│                   파일 탭 + 확장                        │
│                         ▼                               │
│  ┌───────────────────────────────────────────────────┐  │
│  │ content_mode = Reader { ... }                     │  │
│  │ ┌─────────────────────────────────────────────┐   │  │
│  │ │         <Reader /> (오버레이)               │   │  │
│  │ │  - 전체 화면 컨텐츠 표시                    │   │  │
│  │ │  - 닫으면 이전 뷰로 복귀                    │   │  │
│  │ └─────────────────────────────────────────────┘   │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

### 장점

1. **상태 공유**: `fs`, `current_path`, `wallet`이 자동 동기화
2. **독립적 UI**: Terminal과 Explorer가 각자의 UI 로직 유지
3. **오버레이 분리**: Reader는 어느 뷰에서든 열 수 있음
4. **확장성**: 나중에 Editor, Settings 등 추가 용이
5. **뷰 전환 시 상태 유지**: Terminal ↔ Explorer 전환해도 히스토리, 선택 상태 유지
